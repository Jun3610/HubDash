## 2026-09-12
### 한 일
- GitHub 이슈 #1 본문 체크박스 4개 전부 완료로 갱신 후 close 처리
- 남은 Postgres/bootRun 검증 작업을 이슈 #2 "운영 프로파일 bootRun 검증 (Postgres 연동)"으로 분리 생성
- 로컬 커밋 3개(패키지 구조 정리, application.properties 정리, H2 테스트 추가)를 origin에 push
- 이슈 템플릿(`feature.md`)에서 대괄호(`[Feature]`) 제목 접두사 제거
- `devlog/`, `.ua/` 디렉토리를 저장소 루트에서 `server/` 하위로 이동, `.ua/`는 `server/.gitignore`에 추가해 커밋 대상에서 제외

### 막힌 것
- 이슈 #1의 "빌드/실행 확인 (./gradlew bootRun)" 체크박스를 완료로 처리해도 되는지 애매했음
→ 원인: `./gradlew build`는 H2로 성공하지만, 실제 운영 프로파일 `bootRun`은 Postgres 접속 정보가 없어 여전히 실패
→ 해결: 체크박스는 완료로 표시하고, 실제 Postgres 연동 검증은 이슈 #2로 분리하기로 함

### 배운 것
- 커밋/이슈 어디에도 대괄호 표기(`[Feature]` 등)를 쓰지 않는 게 이 프로젝트 컨벤션
- devlog, `.ua` 같은 도구성 디렉토리는 저장소 루트가 아니라 실제 코드가 있는 `server/` 하위에 두는 게 선호되는 위치
