## 2026-09-24 (웹 생활·습관 화면 — 이슈 #102)
### 한 일
- 생활 화면: 학기 다짐 태그, 습관 × 최근 14일 트래커(칸 클릭 = 그날 기록 생성/토글, 최근 7일 달성·연속일), 독서 표와 모달(별점), 오른쪽 오늘 체크·한 줄 메모·주간 달성률·읽는 중인 책
- 실서버 검증(headless Chrome): 빈 이름 검증 → 습관 추가 `POST` 201 → 오늘·3일 전 칸 클릭 → `POST habit-logs` 2건, "2/7 · 연속 1일" → 오른쪽 체크 해제 → `PUT` completed=false → 완독일이 시작일보다 빠르면 검증 → 별점 4로 책 저장. DB에서 기록 값 확인 후 습관·기록·책 모두 삭제(0/0/0 원복)
- 날짜가 바뀌어(09-23 → 09-24) 오늘 칸이 24일로 넘어간 것도 실제로 확인됨

### 막힌 것
- 칸과 오른쪽 체크를 연달아 누르면 잠깐 서로 다른 상태로 보임
→ 원인: 두 컴포넌트가 각자 낙관적 표시를 갖고 있어 첫 저장의 재조회가 끝날 때까지 어긋남
→ 해결: 저장이 끝나면 둘 다 서버 값으로 맞춰지는 걸 시간별로 확인(일시적). 대신 같은 원인으로 가능한 "같은 날 기록 중복 생성"을 막기 위해 저장 중인 (습관, 날짜) 클릭은 무시하도록 수정 → 더블클릭 시 요청 1건

### 배운 것
- 낙관적 업데이트를 컴포넌트마다 두면 표시는 금방 맞춰지지만, 서버 쓰기의 중복은 따로 막아야 한다 — 보이는 상태와 보내는 요청은 별개 문제
