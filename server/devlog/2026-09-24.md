## 2026-09-24 (웹 생활·습관 화면 — 이슈 #102)
### 한 일
- 생활 화면: 학기 다짐 태그, 습관 × 최근 14일 트래커(칸 클릭 = 그날 기록 생성/토글, 최근 7일 달성·연속일), 독서 표와 모달(별점), 오른쪽 오늘 체크·한 줄 메모·주간 달성률·읽는 중인 책
- 실서버 검증(headless Chrome): 빈 이름 검증 → 습관 추가 `POST` 201 → 오늘·3일 전 칸 클릭 → `POST habit-logs` 2건, "2/7 · 연속 1일" → 오른쪽 체크 해제 → `PUT` completed=false → 완독일이 시작일보다 빠르면 검증 → 별점 4로 책 저장. DB에서 기록 값 확인 후 습관·기록·책 모두 삭제(0/0/0 원복)
- 날짜가 바뀌어(09-23 → 09-24) 오늘 칸이 24일로 넘어간 것도 실제로 확인됨

### 막힌 것
- 칸과 오른쪽 체크를 연달아 누르면 잠깐 서로 다른 상태로 보임
→ 원인: 두 컴포넌트가 각자 낙관적 표시를 갖고 있어 첫 저장의 재조회가 끝날 때까지 어긋남
→ 해결: 저장이 끝나면 둘 다 서버 값으로 맞춰지는 걸 시간별로 확인(일시적). 대신 같은 원인으로 가능한 "같은 날 기록 중복 생성"을 막기 위해 저장 중인 (습관, 날짜) 클릭은 무시하도록 수정 → 더블클릭 시 요청 1건

### 배운 것
- 낙관적 업데이트를 컴포넌트마다 두면 표시는 금방 맞춰지지만, 서버 쓰기의 중복은 따로 막아야 한다 — 보이는 상태와 보내는 요청은 별개 문제

---

## 2026-09-24 (웹 일정 화면 — 이슈 #103)
### 한 일
- 일정 화면: 주 보기(종일 줄, 08–22시 격자, 현재 시각선, 겹침 줄 나누기), 월 보기, 목록 보기, 선택 일정 상세와 연결된 리마인더, 다가오는 7일, 추가·수정·삭제
- 실서버 검증(노션에서 옮긴 일정 321건): 이번 주 일정 11개, 다가오는 7일에 추석 연휴·출근 일정 등 표시, 10월 월 보기에 중간고사 종일 일정이 날짜마다 표시. 끝 < 시작 검증 → 14:00–15:30 저장 `POST` 201 → 격자에서 블록 위치가 정확히 336px(=6시간×56px) → 종일로 수정 `PUT`(00:00–23:59) → 종일 줄로 이동 → 리마인더 링크 `/reminders?new=1&domain=schedule&entity=323` → 삭제 `DELETE` 204, DB 321건으로 원복

### 막힌 것
- 월 보기 시각 표시 스타일 누락(CSS 모듈 클래스를 문자열로 씀), 08:00 라벨 잘림 → 각각 수정

### 배운 것
- 배치 로직을 순수 함수로 빼 두니 겹침·자정 넘김·격자 밖 같은 경우를 화면 없이 테스트로 먼저 확인할 수 있었고, 실제 화면에서는 픽셀 위치 하나만 재 보면 충분했다

---

## 2026-09-24 (웹 메모 화면 — 이슈 #104)
### 한 일
- 메모 화면: 목록(검색·태그 필터·미리보기), 편집기(제목·태그·마크다운 편집/미리보기), 0.8초 디바운스 자동 저장과 상태 표시
- 마크다운은 라이브러리 없이 React 요소로 그리는 렌더러로 구현 — innerHTML을 안 써서 메모에 `<script>`가 있어도 글자로만 보임
- 실서버 검증: 제목만 입력 → "내용을 입력하면 저장돼요"(요청 0건) → 본문 입력 → `POST` 1건, 주소 `?id=7` → 계속 입력 → 멈출 때마다 `PUT` 1건(키 입력마다가 아님) → 태그 추가 → 미리보기에서 제목·굵게 렌더, script 요소 0개·alert 없음 → 검색 → 삭제 `DELETE` 204

### 막힌 것
- 방금 만든 메모가 "8시간 전"으로 표시됨
→ 원인: API 컨테이너 시간대가 UTC(`date`로 확인: KST 09:28 = 컨테이너 00:28 UTC). LocalDateTime.now()로 찍는 createdAt/updatedAt이 UTC 벽시계로 저장되는데, 클라이언트는 시간대 없는 값을 로컬(KST)로 읽음 → 9시간 차이
→ 해결: 클라이언트 문제가 아니라 서버 설정 문제라 별도 이슈로 분리

### 배운 것
- 시간대 없는 LocalDateTime은 "누가 어느 시계로 찍었는지"가 약속이어야 한다 — 사용자 입력값(일정 시각)은 KST로 맞았지만 서버가 자동으로 찍는 값만 UTC였다. 목 서버로는 절대 안 드러나는 문제라 실서버 확인이 필요했음

---

## 2026-09-24 (API 컨테이너 시간대 — 이슈 #120)
### 한 일
- 웹 메모 화면 검증 중 발견한 시간 9시간 어긋남을 별도 이슈로 만들어 수정: Dockerfile 런타임에 `TZ=Asia/Seoul`, `-Duser.timezone=Asia/Seoul`
- `docker compose up -d --build app`으로 재기동 → 컨테이너 `date` KST, 새 메모 `createdAt` 09:30:27이 호스트 시각과 일치, 로그에 오류 없음. `./gradlew :api:test` 265개 통과

### 막힌 것
- 방금 만든 메모가 "8시간 전"
→ 원인: 컨테이너 JVM 기본 시간대 UTC → createdAt/updatedAt이 UTC 벽시계로 저장. 로컬 bootRun(맥, KST)에서는 안 보이던 문제
→ 해결: JVM 시간대 고정. 이미 저장된 행(9-23 이관·테스트로 만든 행)의 createdAt/updatedAt은 9시간 이른 값으로 남겨 둠 — 메타데이터라 보정하지 않음(이슈에 기록)

### 배운 것
- 같은 코드라도 "어디서 띄웠는지"(맥 bootRun vs 컨테이너)에 따라 데이터 의미가 바뀔 수 있다 — 시간대 같은 환경값은 이미지에 명시해 두는 게 안전

---

## 2026-09-24 (웹 허브 화면 — 이슈 #105)
### 한 일
- 허브 화면: 카테고리 목록, 링크 카드/목록, 검색, 링크 추가 폼(주소로 제목 후보 채움), 카드 메뉴(사이드바 고정·수정·삭제), 카테고리 관리
- 실서버 검증(노션 이관 카테고리 4개·링크 21개): 노션 링크가 `target=_blank`, `rel=noopener noreferrer`로 실제 새 탭에서 열림(#105 결정대로) → 임시 카테고리 생성 → 주소 "jenkins.io"는 형식 오류 → 전체 주소 입력 시 제목 "Pipeline · jenkins.io" 자동 → 저장 → 고정하면 사이드바에 나타남 → 제목 수정 → 목록 보기 확인 → 삭제하면 고정에서도 빠짐 → 카테고리 삭제. DB 4/21로 원복

### 막힌 것
- React 18에서 ref를 일반 prop으로 넘겨 폼 스크롤이 안 됨 → formRef prop으로 전달

### 배운 것
- 링크 제목 가져오기처럼 "다른 사이트를 읽는" 기능은 브라우저 혼자서는 CORS 때문에 불가능 — 서버 프록시 없이 할 수 있는 만큼(주소에서 추정)으로 범위를 정함

---

## 2026-09-24 (웹 리마인더 화면 — 이슈 #106)
### 한 일
- 리마인더 화면: 대기/보냄/전체 탭, 지남·오늘·예정·보냄 묶음, 보냄 처리·미루기·되돌리기, 새 리마인더 폼(빠른 선택, 연결 대상), 알림 설정 카드
- 실서버 검증: 일정 상세의 링크(`?new=1&domain=schedule&entity=20`, 실제 이관 일정 "CL14 출근")로 들어오면 도메인·대상이 채워지고 제목 칸에 포커스 → 빈 제목 검증 → "마감 하루 전"이 일정 전날 21:00으로 → 저장 `POST` 201 → 대상 링크 "CL14 출근 · 09.26 14:00" 표시 → 지난 시각 알림은 "지남" 묶음 → 보냄 처리 `PUT sent=true` → 보냄 탭 → 되돌리기 → 미루기로 21:00 → 22:00 → 일정 상세에 "리마인더 내일 22:00" 표시. DB 값 확인 후 삭제(0건)
- 이로써 웹 클라이언트 10개 화면(#97~#106)과 기반(#96) 완료

### 배운 것
- 화면 사이를 주소 파라미터(`?event=`, `?new=1&domain=…&entity=…`)로 잇게 해 두니, 일정 → 리마인더 → 일정으로 돌아오는 흐름을 별도 전역 상태 없이 만들 수 있었다

---

## 2026-09-24 (웹 전체 점검과 모바일 헤더 — 이슈 #124)
### 한 일
- main 기준 최종 점검: typecheck·lint(경고 0)·Vitest 99개·build 통과. 실서버에 붙여 11개 경로(없는 주소 포함)를 1440px/390px로 전부 열어 콘솔 오류 0, API 4xx/5xx 0, 가로 넘침 0 확인
- 점검 중 모바일 헤더 문제를 발견해 이슈 #124로 분리해 수정: 탭과 버튼을 두 줄로

### 막힌 것
- 모바일 건강 화면에서 운동·체중수면·주간통계 탭이 보이지 않음 → 헤더가 한 줄 고정이라 버튼에 밀려 숨은 스크롤 뒤로 감 → 모바일에서만 두 줄 배치

### 배운 것
- 화면을 하나씩 만들 때는 데스크톱 캡처 위주로 봤는데, 마지막에 전 화면을 모바일로 한 번에 훑으니 공통 컴포넌트(헤더)의 문제가 한 번에 드러났다 — 공통 레이아웃은 전체 순회로 점검하는 게 효율적

## 2026-09-24 (웹까지 루트 docker compose 하나로 — 이슈 #126)
### 한 일
- `server/docker-compose.yml`을 레포 루트로 옮기고(`git mv`) `web` 서비스 추가 — 루트에서 `docker compose up -d --build` 한 번으로 db, kafka, api, web 4개가 뜸. 서비스 이름 `app`은 역할이 드러나게 `api`로 바꿈
- `client/web/Dockerfile`(node로 빌드 → nginx로 서빙), `nginx.conf`(`/api/`는 `api:8080`으로 프록시, SPA 라우팅 fallback, 해시 붙은 에셋 장기 캐시), `.dockerignore`(로컬 `.env`가 빌드에 섞여 목 서버로 동작하지 않게 제외)
- 웹과 API가 같은 주소(`127.0.0.1:3000`)라 CORS 설정 없이 동작 — 앱은 기본 설정에서 `/api` 상대 경로로 요청하므로 클라이언트 코드는 수정 없음
- 기존 데이터 유지: 볼륨 이름을 `server_hubdash-db-data`/`server_hubdash-kafka-data`로 지정해 그대로 이어 씀. 전환 전 `pg_dump` 백업
- 실제 전환 검증: 기존 `server` 프로젝트 컨테이너를 `down`(볼륨 유지) → 루트에서 `up -d --build`. 새 빈 볼륨이 생기지 않았고 일정 321건 그대로, 웹 `/` 200, SPA 경로 새로고침(`/health`) 200, 에셋 200, `/api` 프록시 200(키 없으면 401), 웹을 거친 raw 메모 → Kafka ETL PROCESSED 확인(검증 메모는 삭제, raw 기록 1건은 이력으로 남음)
- 루트 README에 실행 방법 추가, server/client README의 compose 위치·명령 갱신

### 막힌 것
- compose 파일 위치를 옮기면 프로젝트 이름이 `server` → `hubdash`로 바뀌어 볼륨 이름도 `hubdash_hubdash-db-data`로 새로 만들어짐(기존 데이터가 안 보이게 됨)
→ 원인: compose는 볼륨 이름 앞에 프로젝트 이름(기본값은 compose 파일이 있는 폴더 이름)을 붙임
→ 해결: 프로젝트 이름을 `name: hubdash`로 고정하고, 볼륨은 `name:`으로 기존 이름을 명시
- 같은 `container_name`을 쓰는 기존 컨테이너가 떠 있으면 새 프로젝트에서 이름 충돌
→ 해결: `docker compose -p server down`으로 기존 프로젝트를 먼저 내림(볼륨은 유지)

### 배운 것
- Dockerfile은 이미지 하나를 만드는 법, compose는 여러 이미지를 컨테이너로 같이 띄우는 법 — 앱마다 Dockerfile을 자기 폴더에 두고, 전체를 묶는 compose는 루트에 하나 두는 구조가 자연스럽다
- compose 파일을 옮기는 건 코드 변경이 아니라 데이터 위치 변경일 수 있다 — 볼륨 이름이 프로젝트 이름에 묶여 있다는 걸 모르고 옮기면 데이터가 "사라진 것처럼" 보인다

## 2026-09-24 (이어서, 사용 전 전체 점검과 마무리 — 이슈 #128)
### 한 일
- 9시간 어긋난 `createdAt`/`updatedAt` 보정: 컨테이너가 UTC로 돌던 동안(시간대 수정 a73016c 적용 전, 2026-09-24 00:30:20 UTC 이전) 기록된 값만 +9시간. 모든 테이블의 `created_at`/`updated_at`만 대상이고 사용자가 입력한 시각(일정 시작/끝, 식사 시각, 리마인더 시각)은 제외. 적용 전 `pg_dump` 백업, 수정 직후~09:30 사이의 모호한 행이 0건인지 먼저 확인, 한 트랜잭션으로 적용(일정 321건 등 약 800행). 로컬 DB에만 적용한 데이터 보정이라 마이그레이션 파일은 만들지 않음
- 전체 점검: 서버 테스트 290개, 웹 타입 검사/린트/테스트 99개/빌드 모두 통과. 웹(3000)을 거쳐 클라이언트가 쓰는 API 23종을 전부 호출해 확인(주간 통계 3종은 부모 ID가 필수인데 클라이언트가 넘기는 것까지 확인)
- 문제 발견 → 수정: 주간 통계 배치가 매일 00:10에만 돌아, 필요할 때만 켜서 쓰는 운영에서는 통계가 계속 빈다 → 앱 기동 시(`ApplicationReadyEvent`) 지난주 통계 5종을 한 번 집계(`analytics.batch.run-on-startup`, 테스트 프로파일은 끔). 실제 서버에서 기동/재기동 두 번 모두 5개 COMPLETED 확인(같은 주 재집계도 정상)
- 커밋되지 않았던 `client/hubdash-design/`(화면별 디자인, PROMPT.md — 클라이언트 README와 코드 주석이 참조) 커밋
- 루트 README에 백업/복원 명령 추가 — `pg_dump` 후 임시 DB에 `pg_restore`해서 건수(일정 321)까지 확인하고 임시 DB 삭제

### 막힌 것
- 시간 보정 기준을 어디로 잡을지
→ 원인: 수정 전 값은 UTC, 수정 후 값은 KST라 같은 컬럼에 두 기준이 섞여 있음
→ 해결: 시간대를 고친 컨테이너가 뜬 시각(00:30:20 UTC)을 기준으로 삼음. 그 전 값은 UTC로 저장돼 이 시각보다 작고, 그 후 값은 KST라 09:30 이상이라 사이 구간이 비어 있는 걸 먼저 세어 확인(모호 0건)

### 배운 것
- 스케줄(cron)은 "서버가 계속 켜져 있다"는 가정을 깔고 있다 — 운영 방식을 바꾸면(필요할 때만 켜기) 시간 기반 작업이 조용히 안 돌 수 있어, 기동 시 따라잡기가 같이 필요하다
- 데이터 보정은 "대상 컬럼 구분(시스템 기록 vs 사용자 입력) → 기준 시점 → 모호 구간 0건 확인 → 백업 → 한 트랜잭션"으로 하면 되돌릴 수 있고 설명할 수 있다

---

## 2026-09-24 (웹 사용 피드백 2차 정리 + 냉장고 API — 이슈 #130~#137)
### 한 일
- 실제로 써 본 사용자 피드백을 이슈로 나눔: 냉장고 API(#130), 건강 화면 개선(#131), 사이드바·허브(#132), 히트맵에 지난 일정(#133), 과목 대시보드·성적(#134), 과제·리마인더 제거(#135), 메모 저장·생활 합치기(#136), 체중 기록 시각(#137)
- 냉장고(음식 목록) 서버: health_food(V26) + /api/health/foods CRUD, 고정 먼저 정렬. 저장 위치는 사용자가 서버로 결정(브라우저 저장 X)
- 검증: `./gradlew :api:test` 277개 통과, 루트 compose로 api 재빌드 → flyway 26 적용, 고정한 음식이 먼저 나오고 빈 이름·음수 칼로리는 400, 확인용 데이터 삭제

### 막힌 것
- "최근 1년 기록 1건" — 원인: 히트맵이 식단·운동·체중·공부·습관·완료 과제만 세는데 DB에는 오늘 아침 끼니 1건뿐이고, 1년치 흔적은 노션에서 옮긴 지난 일정 298건에 있었음 → #133에서 지난 일정을 합산하도록
- "탄수 140 이하"는 어디서 왔나 — 디자인 명세(노션 식단 원칙 메모)의 예시값을 기본 목표로 하드코딩했던 것. 사용자가 정한 적 없는 값이 설정처럼 보였음 → #131에서 기본값 없이 사용자가 정하게

### 배운 것
- 명세의 예시값을 기본값으로 넣으면 사용자는 "내가 어디서 설정했지?"라고 느낀다 — 개인 목표 같은 값은 비워 두고 정하게 하는 쪽이 맞다

---

## 2026-09-24 (서버: 과목 성적·메모, 체중 기록 시각, 식단 목표 — 이슈 #134·#137·#131)
### 한 일
- 과목 grade/memo(V28), 체중·수면 recorded_at 시각까지(V27), 식단 목표 단일 행 API(V29)
- 사용자 방향 전환 반영: 식단은 음식 목록 대신 끼니(아침·점심·저녁·간식)마다 제목 + 탄단지만 적고 칼로리는 자동 계산 → 서버 끼니 모델은 그대로 쓰고(끼니 1건 = 항목 1건), 방금 만든 냉장고 API(#130)는 화면에서 쓰지 않기로
- 검증: `./gradlew :api:test` 289개 통과(간헐 실패 4건은 #139로 분리). 루트 compose로 api 재빌드 → flyway 27~29 적용, recorded_at timestamp, 목표 처음엔 전부 null·저장·잘못된 규칙 400, 체중 07:35 저장 후 주간 통계 집계, 임시 과목으로 성적 B+·메모 저장과 잘못된 등급 E 거부 확인. 테스트로 만든 과목·체중 기록 삭제, 목표는 다시 비움. 사용자가 직접 넣은 체중 기록 1건(77.5kg)은 그대로 두고 00:00으로 옮겨진 것 확인

### 막힌 것
- 주간 통계 쿼리가 BETWEEN(날짜) 기반이라 시각이 붙으면 일요일 기록이 빠질 상황 → 반열린 구간으로 바꾸고 경계 테스트 추가
- 전체 테스트 간헐 실패(user_profile/user_setting 테이블 없음) → 이번 변경과 무관, 이슈 #139

### 배운 것
- 사용자 요구가 대화 중에 계속 바뀔 때는(냉장고 → 끼니별 탄단지) 서버 모델을 바꾸기 전에 기존 모델로 표현 가능한지 먼저 본다 — 이번엔 끼니 1건 = 항목 1건으로 서버 변경 없이 흡수

---

## 2026-09-24 (웹: 과제·리마인더 제거, 건강 화면 개편 — 이슈 #135·#131·#137)
### 한 일
- 과제·리마인더를 화면에서 모두 제거(서버 데이터는 유지), 홈 KPI를 "이번 주 운동"으로 교체 (PR #141)
- 건강 화면 개편: 통계 첫 화면(탄단지 누적 칼로리·7일 평균·체중·수면·주간 평균·운동), 끼니마다 제목 + 탄단지만 적고 칼로리 자동 계산, 식단 목표를 서버에 두고 건강 화면에서 설정, 나트륨 제거, 체중 기록 시:분
- 실서버 검증(headless Chrome): 첫 탭이 통계, 목표 음수 검증 → 탄수 140 이하·단백질 160 이상·권장 1500 저장 → 점심 제목 없이 저장 시 검증 → 58/8/42g 입력 시 472kcal 자동, 저장 후 합계 "탄수 108g 140g 이하 ✓ · 권장 1,500kcal 이하 ✓" → 단백질 50g으로 수정 시 504kcal → 체중 21:45 기록. 사용자의 실제 아침(예전 방식 음식 2개)은 한 카드로 합쳐 보이는 것만 확인하고 건드리지 않음. 테스트 점심·체중 기록 삭제, 목표 다시 비움

### 막힌 것
- 과목 수정 창이 서버에 새로 생긴 grade·memo를 안 보내 저장 시 지워질 상황 → 수정 요청에 기존 값을 그대로 포함(#135 PR에서 같이 처리)

### 배운 것
- 서버 PUT이 전체 교체일 때 필드가 하나 늘 때마다 클라이언트 수정 폼도 같이 챙겨야 한다 — notionUrl에 이어 두 번째. 다음부터는 서버 DTO에 필드를 추가하는 PR에 "웹 수정 요청에 포함" 체크를 남긴다
