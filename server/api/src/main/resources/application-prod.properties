# 운영(prod) 프로파일 — 시크릿에는 기본값을 두지 않는다.
# application.properties의 개발용 기본값(API_KEY:dev-local-key, DB_PASSWORD:hubdash)이 배포에서 조용히 적용되면
# 누구나 아는 키로 API가 열리므로, 환경변수를 빠뜨리면 기동 자체가 실패하도록 한다(Could not resolve placeholder).
app.api-key=${API_KEY}
spring.datasource.password=${DB_PASSWORD}
